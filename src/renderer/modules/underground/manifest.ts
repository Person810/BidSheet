import type { TradeModule } from '../types';

export const undergroundModule: TradeModule = {
  id: 'underground',
  name: 'Underground Utility',
  icon: '\u{1F527}',  // wrench emoji -- swap to SVG/Lucide later if desired
  tools: [],           // trench profiler, plan takeoff, etc. go here
};
