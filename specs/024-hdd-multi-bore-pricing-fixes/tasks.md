# Tasks: HDD Multi-Bore Pricing & Dynamic Rates Fixes

- [x] Task 1: Rework `calculateHDD` to accept length in canonical feet, converting it internally to meters for metric calculations.
  - [x] Implement length conversion inside `calculateHDD` in `src/renderer/modules/underground/hddCalc.ts`.
  - [x] Rework validation `validateHDDInput` and boundary check bounds to correspond to correct units.
  - [x] Fix the production rate math (use division instead of multiplication).
- [x] Task 2: Update unit tests in `src/renderer/modules/underground/hddCalc.test.ts`.
  - [x] Pass lengths in canonical feet.
  - [x] Add a unit test verifying `customRates` outputs affect calculations.
- [x] Task 3: Add `migrateV50` database migration in `src/main/database.ts`.
  - [x] Alter table `trench_profiles` to add `hdd_bores_per_pit` and `hdd_additional_pipes_json`.
  - [x] Append to migrations array.
- [x] Task 4: Whitelist the settings syncing.
  - [x] Add `hdd_rates_json` to `SYNCED_SETTINGS` whitelist in `src/main/cloud/catalog-sync.ts`.
- [x] Task 5: Rework frontend forms to use the new columns instead of backfillType serialization.
  - [x] Update `TrenchProfileForm.tsx` to read/write `hdd_bores_per_pit` and `hdd_additional_pipes_json`.
  - [x] Update `TrenchProfileList.tsx` to read/write `hdd_bores_per_pit` and `hdd_additional_pipes_json`.
  - [x] Wire `customRates` into the calls to `calculateHDD` in both files.
- [x] Task 6: Localize HDDRatesModal standard system default active tab.
  - [x] Read active profile locale or unit system to default the active tab in `HDDRatesModal.tsx`.
- [x] Task 7: Fix settings page saving.
  - [x] Ensure that updating rates immediately saves or stages correctly in `SettingsPage.tsx`.
- [x] Task 8: Run quality gates.
  - [x] Verify that `npm run typecheck` passes.
  - [x] Verify that `npx vitest run` passes.
